{
  "buildCommand": "cd ../.. && npm install && npm run build -w apps/web",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/((?!.*\\.[a-zA-Z0-9]+$).*)",
      "destination": "/index.html"
    }
  ]
}
