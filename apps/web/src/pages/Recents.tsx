export default function Recents() {
  return (
    <div className="empty-state">
      <h2>Recents</h2>
      <p>Your recent calls will appear here.</p>
      <p className="muted">(Phase 4.5 — populated once SIP is wired.)</p>
    </div>
  );
}
