# Copy this file to .env (locally) or paste each value into Render's
# "Environment" tab per service. Never commit a real .env file.

# ----- Database (Supabase Postgres) -----
# Get these from Supabase project: Connect button or Settings -> Database
DATABASE_URL="postgresql://postgres.xxxxx:[PASSWORD]@aws-0-us-east-1.pooler.supabase.com:6543/postgres"
DIRECT_DATABASE_URL="postgresql://postgres.xxxxx:[PASSWORD]@aws-0-us-east-1.pooler.supabase.com:5432/postgres"

# ----- Cache / Queue (Upstash Redis) -----
# From Upstash dashboard, "Details" tab
REDIS_URL="rediss://default:[PASSWORD]@xxxxx-us1.upstash.io:6379"

# ----- Telnyx -----
TELNYX_API_KEY="KEY..."
TELNYX_PUBLIC_KEY=""           # used to verify webhook signatures
TELNYX_MESSAGING_PROFILE_ID="" # filled in when first number is bought

# ----- JobDiva -----
JOBDIVA_USERNAME="as@aptask.com"
JOBDIVA_PASSWORD=""
JOBDIVA_CLIENT_ID=""
JOBDIVA_BASE_URL="https://api.jobdiva.com"

# ----- ElevenLabs -----
ELEVENLABS_API_KEY="sk_..."

# ----- Storage (Supabase Storage initially; switch to S3 later) -----
S3_BUCKET=""
S3_REGION="us-east-1"
S3_ACCESS_KEY_ID=""
S3_SECRET_ACCESS_KEY=""

# ----- Auth -----
JWT_SECRET=""                 # any random 32+ character string for v0.1
JWT_EXPIRES_IN="15m"
JWT_REFRESH_EXPIRES_IN="30d"

# ----- Sentry (optional) -----
SENTRY_DSN=""

# ----- Service config -----
# Render injects PORT automatically. Locally, each service has its own default.
PORT=3000
NODE_ENV=development
LOG_LEVEL=info

# ----- Allowed origins for CORS (comma-separated) -----
ALLOWED_ORIGINS="http://localhost:5173,https://dialer.aptask.com"
